# טביעת-אצבע ופרווננס — מודל Chordix

> קובץ זה נמצא ב-`build/` ולכן **לא מתפרסם** לאתר (מוחרג מ-`dist/`). הוא הוכחת-בעלות
> על משקלי המודל, למקרה של אתר-מתחרה שיעתיק את הקובץ. אינו סוד — פרסום ה-hash דווקא
> מחזק הוכחת-קדימות.

## שכבה 1 — פרווננס (הוכחת-קדימות)
- **קובץ:** `chordix-model-e1.bin`
- **SHA-256:** `5f5cc590e486b286ac5ef4344a01e021358c74572731cee75601b2a056f7ced8`
- **גודל:** 2,846,392 בייט
- **קומיט ראשון בגיט (חותמת-זמן):** `6af6ec2f6b1855bc49909cbb0e5101bf018c71b0` — 2026-09-10 20:31:45 +0300

**מה זה נותן:** git+GitHub חותמים-בזמן את הקומיט הזה. אם מתחרה יפרסם את אותם משקלים,
ה-SHA-256 הזהה + התאריך המוקדם שלנו = ראיה שהמודל שלנו קדם. לחיזוק עצמאי (עוגן בלוקצ'יין
בלתי-תלוי ב-GitHub): להריץ **OpenTimestamps** על ה-hash —
`ots stamp` על קובץ שמכיל את ה-hash, לשמור את קובץ ה-`.ots`. (פעולה חד-פעמית של המשתמש.)

## שכבה 2 — קנרי התנהגותי (לביצוע אחרי אימות הערפול)
קלט-אודיו **סודי** שמפיק חתימת-פלט ייחודית שרק המודל המדויק שלנו מייצר. אם אתר-מתחרה
מחזיר את אותה חתימה על אותו קלט — ראיה חזקה לגניבה (שורדת שינוי-שם/re-gzip של הקובץ).

**ייצור (חד-פעמי, דורש ריצת-מודל אמיתית — בדפדפן על האתר, או offline):**
1. לייצר קליפ-אודיו סודי קצר (למשל רצף אקורדים/רעש ייחודי, ~10-20ש') — **לא לפרסם**.
2. להריץ אותו דרך המנוע, ולשמור את וקטור-הפלט הגולמי (logits/רצף-האקורדים לפני עיגול).
3. לשמור כאן, **בפרטי בלבד** (לא בריפו אם הוא ציבורי): hash של הקלט + חתימת-הפלט.

> ⚠️ הקלט הסודי והחתימה **לא** נכנסים לריפו ציבורי. אם הריפו פרטי — אפשר; אחרת לשמור
> מקומית/בכספת. סטטוס: **טרם הופק** (ממתין לאימות הערפול חי).
