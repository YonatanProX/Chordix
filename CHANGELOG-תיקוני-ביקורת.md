# Chordix — תיקונים לפי הביקורת

כל תיקון כאן נובע מבאג ממשי בקוד, לא מכוונון "לפי תחושה".
הקבצים שהשתנו: `index.html`, `sw.js` (במפורש — גרסת המטמון).
`chordix-model.bin` **לא** נגעתי בו (אותו md5, אותו גודל).

---

## ציר זמן

### 1. "חלק מהאקורדים לא רשומים בריבוע שלהם אבל כן למעלה"

**הסיבה.** ב-`renderTimeline()` הייתה השורה:

```js
<span>${W>5?txt:''}</span>
```

`W` הוא רוחב הריבוע באחוזים מרוחב השורה. כל אקורד שתפס פחות
מ-5% — בזום ברירת המחדל של 16 שניות לשורה, כלומר כל אקורד קצר
מ-0.8 שנייה — נצבע כריבוע **בלי שם בכלל**. "מתנגן" למעלה כן הראה
אותו, ולכן זה נראה כמו חוסר עקביות ולא כמו מגבלת מקום.

**התיקון.** לכל ריבוע יש שם, תמיד. הכתב קטן בהדרגה לפי הרוחב
(14px → 12px → 10px), ובריבוע שבאמת אין בו מקום לרוחב — השם
נכתב אנכית. מוסיפים לזה מדרגה נוספת של הקטנה במסכי טלפון.
ריבוע שנחתך בין שתי שורות מסומן ב-`·` קטן בהתחלה, כדי שיהיה
ברור שזה המשך ולא אקורד חדש.

### 2. "לא מזהה חלק מהאקורדים"

**הסיבה.** ציר הזמן פוענח **בנפרד לגמרי** מדף האקורדים
(`viterbiFast` ברמת הפריים + `runsFromPath`), ולכן לא ראה כלום
מההשלמה המבנית — המנגנון שמחבר את הראיות מכל המופעים של אותו
מקטע וכותב אקורד גם היכן שבמופע הזה הוא נחבא מאחורי שירה או
תופים. כל מה שהמנגנון הזה מצא הופיע בדף ולא בציר הזמן. בנוסף
`runsFromPath` מיזג בכוח כל ריצה קצרה מ-0.30 שנייה לתוך שכנתה.

**התיקון.** ראו סעיף 3 — ציר הזמן יורש עכשיו את כל שרשרת
העיבוד של הדף.

### 3. "אקורדים לא מדויקים לעומת ציר הזמן"

**הסיבה — וזה היה הבאג המרכזי.** שתי התצוגות פוענחו משני
מסלולים עצמאיים:

| | דף האקורדים | ציר הזמן (קודם) |
|---|---|---|
| פענוח | `viterbiFull` ברמת הפעימה | `viterbiFast` ברמת הפריים |
| ידע על הסולם | כן (`keyPriorVector`) | חלקי |
| השלמה מבנית | כן | **לא** |
| היפוכים (C/E) | כן | **לא** |
| פאוור-אקורדים | כן | **לא** |
| תיקונים ידניים | כן | נפרדים |

שתי אמיתות לאותה הקלטה. המשתמש לא יכול לדעת לאיזו להאמין.

**התיקון.** נוספה `runsFromBars()`: ציר הזמן נבנה מהתיבות
הסופיות, אחרי כל שלבי העיבוד. הרזולוציה התת-פעימתית לא אבדה —
`refineRunEdges()` מזיז את *קו ההחלפה* (לא את זהות האקורד)
בתוך חלון של פחות מחצי פעימה, לנקודה שבה הראיה ברמת הפריים
באמת עוברת מהאקורד היוצא לנכנס. כך נתפסות הקדמות של נגנים.
ההזזה מתבצעת רק כשהראיה טובה יותר במרווח מובהק, ולכן על
פוסטריור רועש היא לא זזה בכלל (נבדק).

`runsFromPath` ו-`viterbiFast` נשארו בקוד עם הערה שהם כבר לא
במסלול הראשי — הם עצמאיים ושימושיים לבדיקות.

### 4. "האקורדים לא עוברים בזמן"

שני גורמים שונים, שניהם טופלו:

* **פנימי.** הגבולות של ציר הזמן היו מקוונטים לפריימים
  (~93ms) ולא נחו על פעימות. עכשיו הם מגיעים מרשת הפעימות
  ומדויקים תת-פעימתית.
* **חיצוני, ולא ניתן לתיקון בניתוח.** אוזניות בלוטות' מוסיפות
  100–300ms של השהיית פלט. שום זיהוי לא יתקן את זה. לכן נוסף
  מחוון **"סנכרון"** (±400ms) בראש ציר הזמן, שמזיז את ההדגשה
  בשתי התצוגות ונשמר ב-localStorage.

---

## דף אקורדים

### 5. "מראה כמה אקורדים באותה תיבה למרות שהם לא קשורים"

**הסיבה.** הפענוח ברמת הפעימה רשאי להחליף אקורד בכל פעימה, ולכן
די בפעימה בודדת שהמודל התנדנד בה — ווקאל חזק, מעבר בס, קישוט של
הגיטרה — כדי שהתיבה תיכתב "C G". הקוד הישן מיזג מקטעים רק כאשר
מספרם *חרג* מהמותר (2 כברירת מחדל), אז אקורד רפאים אחד בתיבה
תמיד שרד.

**התיקון.** `tidyBarChords()` — אקורד שני בתיבה נשאר רק אם יש לו
סיבה מוזיקלית:

* אורך מספיק ביחס להגדרת "אקורדים בתיבה", **וגם** יושב על
  פעימה חזקה (1 או 3 ב-4/4) — או שהוודאות בו ≥ 0.45;
* או שהוא **מקדים** את התיבה הבאה (הקדמה — תופעה אמיתית ונפוצה);
* או שהוא **ממשיך** את סוף התיבה הקודמת.

כל השאר נבלע לתוך השכן החזק שלו. זו לא המצאה של מידע: המידע
נשאר בדיוק כפי שהיה, רק בלי החלפות שאיש לא ניגן. מי שכן רוצה
רזולוציה עדינה יותר יכול לבחור "עד ארבעה" — הסף נגזר מהבחירה
ולא קבוע בקוד.

### 6. "האקורדים לא עוברים בזמן"

**הסיבה.** `trackPlayback()` הדגיש **תיבה שלמה**
(`.cellbar.playing`). בתיבה עם שני אקורדים ההדגשה פשוט לא זזה
כשהאקורד התחלף — היא נשארה על אותה תיבה ארבע פעימות.

**התיקון.** `stampChordTimes()` נותן לכל אקורד `t0`/`t1` בשניות
משלו, `barCellHTML()` עוטף כל אקורד ב-`<span class="cd">` נפרד,
ומה שמאיר עכשיו הוא **האקורד**, לא התיבה. החיפוש בכל פריים ציור
הוחלף לחיפוש בינארי במקום סריקה לינארית על כל התיבות.

---

## תיקונים נלווים שהתגלו בדרך

* **היפוכים נמחקו בשקט.** `annotateBass` רץ *לפני* ההשלמה
  המבנית, וזו כותבת את האקורדים מחדש — כלומר כל C/E שסומן נמחק
  ברגע שהמקטע עבר השלמה. הועבר לרוץ אחריה, כמו הפאוור-אקורדים.
* **הדגשה שנעלמה אחרי ציור מחדש.** שינוי זום ציר קרא
  ל-`renderTimeline()` בלי לאפס את `_nowIdx`, ולכן התיבה
  המתנגנת נשארה בלי הדגשה עד ההחלפה הבאה.
* **תיקון ידני לא עבר בין התצוגות.** עכשיו עריכה בדף מתעדכנת
  מיד בציר הזמן, ולהפך (בכל תיבה שהריצה מכסה במלואה).
* **`updateNowPlaying` הדגיש ריבוע אחד בלבד** מבין הריבועים של
  אותו אקורד כשהוא נחתך בין שורות (`querySelector` במקום
  `querySelectorAll`).
* **פס הנגינה** יכול היה לצאת מגבולות השורה בסוף השיר.
* **`sw.js`**: גרסת המטמון הועלתה ל-`chordix-v2`, אחרת גולשים
  חוזרים היו מקבלים את ה-HTML הישן מהמטמון.

---

## מה נבדק

* **תקינות תחביר** — כל שמונה בלוקי ה-JS בדף עוברים `node --check`.
* **בדיקת חוסן** — 400 "שירים" אקראיים × 3 הגדרות "אקורדים
  בתיבה" (1/2/4), כולל מקטעי "אין אקורד": אין תיבה שחורגת
  מהמותר, סכומי הפעימות תקינים, לכל אקורד זמנים חוקיים בתוך
  התיבה, ובריצות ציר הזמן אין חורים, חפיפות, ריצות ריקות או
  שתי ריצות זהות צמודות.
* **דיוק הגבולות** — על ראיה סינתטית שבה ההחלפה האמיתית ב-5.85
  שנייה והפעימה ב-6.00, הגבול נחת בדיוק על 5.85; על פוסטריור
  אחיד (רעש) הוא לא זז בכלל.
* **בדיקת DOM (jsdom)** — שיר בן 17 ריצות: **0 ריבועים בלי
  שם** (לפני התיקון היו כאלה), מחלקות הגודל מוחלות כצפוי,
  "מתנגן"/"הבא" נכונים, ו-`barAt`/`chordAt` בדף מצביעים על
  אותו אקורד שציר הזמן מדגיש באותו רגע.
* **נתונים ישנים** — רשומת היסטוריה בלי זמנים לכל אקורד נטענת
  ומתפרקת נכון לריצות (חלוקה לפי פעימות, ואם אין — שווה).

## מה לא נגעתי בו

פרמטרי הפענוח (`VIT`) נבחרו בסריקת רשת מול מאגר בדיקה. בלי
המאגר הזה כל שינוי שלהם הוא ניחוש שיכול לשפר שיר אחד ולהרוס
חמישה. אם תרצה לשפר את הזיהוי עצמו — זה הכיוון, אבל צריך למדוד.
