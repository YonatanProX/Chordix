# תלויות בנייה — לא נדחפות (Cloudflare מריץ npm ci מתוך package-lock)
node_modules/

# פלט הבנייה — נוצר ע"י `npm run build` (Cloudflare מייצר ומגיש אותו)
dist/

# סודות ומטמון מקומיים של wrangler / Pages Functions — לעולם לא בריפו
.dev.vars*
.env*
.wrangler/
